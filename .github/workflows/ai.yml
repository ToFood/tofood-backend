name: RAICO Review

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  pull-requests: write
  contents: write

env:
  AI_PROVIDER: "gemini"
  AI_MODEL: "gemini-1.5-flash-latest"
  PROMPT_TEXT: "Com base nas alterações do meu PR, consegue me dar dicas de boas práticas de segurança e estilo de código, considerando que este projeto é um [descrição do projeto]?  Detalhes das alterações: [link para as alterações do PR]"

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:

      - name: 🤖 Run Pull Request Review
        uses: ohntrebor/raico/.github/actions/analyze-pr@main
        with:
          api_key: ${{ secrets.GEMINI_API_KEY }}
          ai_provider: ${{ env.AI_PROVIDER }}
          ai_model: ${{ env.AI_MODEL }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          prompt_text: ${{ env.PROMPT_TEXT }}

