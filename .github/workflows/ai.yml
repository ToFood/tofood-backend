name: RAICO Analyze

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  pull-requests: write
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
      - name: 🐈‍⬛ Run Pull Request Analysis
        uses: ohntrebor/raico/.github/actions/analyze-pr@ra/feat/gemini
        with:
          ai_api_key: ${{ secrets.GEMINI_API_KEY }}
          ai_provider: "gemini"
          ai_model: "gemini-1.5-flash-latest"
          github_token: ${{ secrets.GITHUB_TOKEN }}
          prompt_text: |
            Analise este Pull Request. Verifique:
            - Melhorias de código e aderência às boas práticas.
            - Possíveis bugs ou inconsistências.
            - Sugestões de melhorias.
