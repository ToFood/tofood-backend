name: RAICO Analyze

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  pull-requests: write
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
      - name: 🐈‍⬛ Run Pull Request Analysis
        uses: ohntrebor/raico/.github/actions/analyze-pr@ra/modularize
        with:
          api_key: ${{ secrets.OPENAI_API_KEY }}
          ai_provider: "openai"
          ai_model: "gpt-4"
          github_token: ${{ secrets.GITHUB_TOKEN }}
          prompt_text: "Qual é a melhor linguagem para codar?"
